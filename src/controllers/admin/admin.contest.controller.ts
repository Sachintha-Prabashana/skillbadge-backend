import { Request, Response } from 'express';


export const createWeeklyContest = async (req: Request, res:Response) => {
    // try{
    //     const { title, }
    //
    // } catch(error){
    //     console.error("Create Weekly Contest Error:", error);
    //     res.status(500).json({ message: "Server error" });
    // }
}